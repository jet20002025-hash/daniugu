# 🚀 创建超级用户指南

## 📋 概述

本指南介绍如何在 Render 环境中创建或更新超级用户账户（用户名：`super`，密码：`superzwj`）。

---

## 🎯 方法一：通过 API 调用（推荐）

### 步骤 1：设置密钥（可选）

在 Render Dashboard → Environment Variables 中添加：
- Key: `SUPER_USER_SECRET`
- Value: `your-secret-key-here`（自定义密钥）

如果不设置，默认密钥为 `create_super_user_2024`。

### 步骤 2：调用 API

使用 `curl` 或任何 HTTP 客户端：

```bash
curl -X POST https://daniugu.onrender.com/api/admin/create_super_user \
  -H "Content-Type: application/json" \
  -d '{
    "secret": "create_super_user_2024",
    "username": "super",
    "password": "superzwj",
    "email": "super@admin.com"
  }'
```

**参数说明**：
- `secret`: 密钥（默认 `create_super_user_2024`，或使用环境变量 `SUPER_USER_SECRET`）
- `username`: 用户名（默认 `super`）
- `password`: 密码（默认 `superzwj`）
- `email`: 邮箱（默认 `super@admin.com`）

**成功响应**：
```json
{
  "success": true,
  "message": "超级用户已创建成功",
  "user": {
    "username": "super",
    "email": "super@admin.com",
    "is_vip": true,
    "is_super": true
  }
}
```

---

## 🎯 方法二：通过 Render Shell 执行脚本

### 步骤 1：打开 Render Shell

1. 访问 https://dashboard.render.com
2. 选择你的 Web 服务（如 `daniugu`）
3. 点击 **"Shell"** 标签
4. 打开 Shell 终端

### 步骤 2：执行脚本

在 Shell 中执行：

```bash
cd /opt/render/project/src
python create_super_user.py super superzwj super@admin.com
```

**参数说明**：
- 第一个参数：用户名（默认 `super`）
- 第二个参数：密码（默认 `superzwj`）
- 第三个参数：邮箱（默认 `super@admin.com`）

**输出示例**：
```
============================================================
🚀 创建/更新超级用户
============================================================

📊 当前用户数: 5
⚠️  用户 'super' 已存在，将更新密码和权限...
✅ 密码已更新
✅ 权限已更新：is_vip=True, is_super=True
✅ 邮箱已更新：super@admin.com

💾 保存用户数据到存储...
✅ 用户数据已保存成功！

============================================================
✅ 超级用户创建/更新完成！
============================================================

📋 用户信息：
   用户名: super
   密码: superzwj
   邮箱: super@admin.com
   权限: VIP + 超级用户
   状态: 激活

🔗 登录地址: https://daniugu.onrender.com

💡 提示：你现在可以使用这个账户登录系统，拥有所有功能权限！
```

---

## ✅ 验证超级用户

### 1. 登录测试

访问 https://daniugu.onrender.com，使用以下凭据登录：
- 用户名: `super`
- 密码: `superzwj`

### 2. 验证权限

登录后，你应该能够：
- ✅ 使用所有 VIP 功能
- ✅ 使用所有超级用户功能
- ✅ 无限制扫描
- ✅ 访问管理员页面（`/cs`）

---

## 🔒 安全提示

### 1. 更改默认密钥

创建超级用户后，建议：
1. 在 Render Dashboard → Environment Variables 中设置 `SUPER_USER_SECRET`
2. 使用强密钥（至少 32 个字符）
3. 不要将密钥提交到 Git 仓库

### 2. 删除 API 端点（可选）

如果不再需要创建超级用户，可以：
1. 注释掉 `/api/admin/create_super_user` 端点
2. 或添加 IP 白名单限制

---

## 🐛 常见问题

### 问题 1：API 调用返回 403

**原因**：密钥验证失败

**解决**：
- 检查 `secret` 参数是否正确
- 确认环境变量 `SUPER_USER_SECRET` 是否设置
- 如果不设置环境变量，使用默认密钥 `create_super_user_2024`

### 问题 2：Shell 脚本执行失败

**原因**：路径不正确或模块导入失败

**解决**：
1. 确认当前目录：`pwd`
2. 确认脚本存在：`ls -la create_super_user.py`
3. 如果不存在，从 GitHub 拉取最新代码

### 问题 3：用户创建成功但无法登录

**原因**：密码哈希或存储问题

**解决**：
1. 检查 Render 日志，查看是否有错误
2. 确认 Redis 连接正常
3. 重新执行创建脚本

---

## 📝 快速命令参考

### API 调用（最简单）

```bash
curl -X POST https://daniugu.onrender.com/api/admin/create_super_user \
  -H "Content-Type: application/json" \
  -d '{"secret": "create_super_user_2024"}'
```

### Shell 脚本

```bash
python create_super_user.py
```

---

## ✅ 完成！

创建成功后，你可以：
- ✅ 使用 `super` / `superzwj` 登录
- ✅ 拥有所有功能权限
- ✅ 管理其他用户
- ✅ 访问管理员页面

如有问题，查看 Render Dashboard → Logs 获取详细错误信息。
