# 🚀 快速开始 - daniugu.online

## 📋 信息

- **域名**: daniugu.online
- **GitHub**: https://github.com/jet20002025-hash/daniugu
- **服务器**: 阿里云服务器

## ⚡ 三步快速部署

### 步骤 1: 推送代码到 GitHub

在本地执行：

```bash
cd /Users/zwj/股票分析
bash 推送到GitHub.sh
```

或者手动：

```bash
git init
git add .
git commit -m "Initial commit: 大牛股分析系统"
git remote add origin https://github.com/jet20002025-hash/daniugu.git
git branch -M main
git push -u origin main
```

### 步骤 2: 在服务器部署

```bash
# 1. 上传部署脚本到服务器
scp 服务器部署脚本_完整版.sh root@你的服务器IP:/root/

# 2. SSH 连接服务器
ssh root@你的服务器IP

# 3. 执行部署脚本
cd /root
chmod +x 服务器部署脚本_完整版.sh
bash 服务器部署脚本_完整版.sh
```

### 步骤 3: 配置域名和 SSL

#### 3.1 配置 DNS 解析（阿里云）

1. 登录阿里云控制台
2. 进入 **域名** → **域名解析**
3. 选择 `daniugu.online`
4. 添加 A 记录：
   - 记录类型: A
   - 主机记录: @
   - 记录值: 你的服务器IP

#### 3.2 配置 SSL 证书（服务器上）

```bash
# 安装 Certbot
yum install certbot python3-certbot-nginx -y
# 或
apt install certbot python3-certbot-nginx -y

# 获取证书
certbot --nginx -d daniugu.online -d www.daniugu.online
```

## ✅ 完成！

访问 https://daniugu.online 即可使用！

## 📚 详细文档

- [部署完整指南.md](./部署完整指南.md) - 完整部署步骤
- [阿里云服务器部署指南.md](./阿里云服务器部署指南.md) - 详细技术文档

## 🔄 更新代码

```bash
# 在服务器上
cd /var/www/stock-analyzer
git pull origin main
systemctl restart stock-analyzer
```

