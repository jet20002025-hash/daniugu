# 本地测试账号说明

## 测试账号信息

**用户名：** `test`  
**密码：** `test123`  
**邮箱：** `test@local.com`  
**用户等级：** VIP（可以随时扫描，无限制）

## 快速登录

### 方法1：使用测试账号登录
1. 访问登录页面
2. 输入用户名：`test`
3. 输入密码：`test123`
4. 点击登录

### 方法2：自动创建（如果账号不存在）
- 在本地环境使用 `test` / `test123` 登录时，系统会自动创建该账号
- 无需手动注册

## 创建测试账号脚本

如果需要手动创建或重置测试账号，可以运行：

```bash
python3 create_local_test_user.py
```

## 注意事项

1. **仅限本地使用**：此测试账号仅在本地开发环境有效
2. **VIP权限**：测试账号为VIP用户，拥有所有功能权限
3. **数据存储**：账号信息保存在 `users.json` 文件中
4. **不会推送到GitHub**：`users.json` 已在 `.gitignore` 中，不会提交到仓库

## 功能特点

- ✅ 快速登录，无需注册
- ✅ VIP用户权限，无扫描限制
- ✅ 自动创建，首次登录自动生成
- ✅ 仅限本地环境使用





