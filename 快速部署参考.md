# 🚀 快速部署参考

## 一键部署

```bash
cd /Users/zwj/股票分析
bash 一键部署.sh
```

## 手动部署步骤

### Vercel（推荐）

1. **推送代码到 GitHub**
   ```bash
   git add .
   git commit -m "准备部署"
   git push origin main
   ```

2. **在 Vercel 部署**
   - 访问 https://vercel.com
   - 登录 → New Project → 选择仓库 → Deploy

3. **配置环境变量**（Settings → Environment Variables）
   ```
   UPSTASH_REDIS_REST_URL=https://your-redis.upstash.io
   UPSTASH_REDIS_REST_TOKEN=your-token-here
   INVITE_CODES=ADMIN2024,VIP2024
   ```

4. **重新部署**（使环境变量生效）

### Render

1. **推送代码到 GitHub**（同上）

2. **在 Render 部署**
   - 访问 https://render.com
   - 登录 → New + → Web Service → 选择仓库
   - Start Command: `gunicorn bull_stock_web:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120`

3. **配置环境变量**（Environment 标签）
   ```
   UPSTASH_REDIS_REST_URL=https://your-redis.upstash.io
   UPSTASH_REDIS_REST_TOKEN=your-token-here
   RENDER=true
   INVITE_CODES=ADMIN2024,VIP2024
   ```

## 获取 Upstash Redis

1. 访问 https://upstash.com
2. 注册/登录
3. Create Database → 选择区域 → Create
4. 复制 REST API 的 URL 和 Token

## 验证部署

1. 访问部署地址
2. 使用邀请码 `ADMIN2024` 注册
3. 登录并测试扫描功能

## 常见问题

- **超时**: Vercel 免费版 10 秒限制，升级 Pro（$20/月）或使用 Render
- **数据丢失**: 配置 Upstash Redis
- **邀请码无效**: 配置 `INVITE_CODES` 环境变量

## 详细文档

- 完整指南: `部署指南.md`
- Vercel 配置: `Vercel环境变量配置指南.md`
- 持久化存储: `持久化存储配置说明.md`
